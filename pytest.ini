[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    -ra
markers =
    unit: fast, pure-logic tests (no network, no real I/O)
    integration: exercises wiring across modules
